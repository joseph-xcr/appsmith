services:
  - type: web
    name: appsmith
    env: docker
    plan: free
    repo: https://github.com/YOUR_USERNAME/appsmith
    branch: release
    dockerfilePath: deploy/docker/Dockerfile
    envVars:
      - key: BASE
        value: appsmith/appsmith-ce:release
